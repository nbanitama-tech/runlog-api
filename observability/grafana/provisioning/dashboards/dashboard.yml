apiVersion: 1

providers:
  - name: RunLog
    orgId: 1
    folder: RunLog
    folderUid: runlog
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards